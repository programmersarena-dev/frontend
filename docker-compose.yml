version: "3.8"

services:
  frontend:
    build: .
    container_name: react-app
    ports:
      - "3000:80"
    environment:
      VITE_API_URL: http://localhost:8000/api
    restart: unless-stopped
